#	$ssdlinux: Makefile,v 1.5 2003/06/10 03:14:08 yamagata Exp $

.include <bsd.own.mk>

SUBDIR+= sh bash csh ed

# procps
SUBDIR+= ps kill

# usagi-tool/src/netkit-rsh
SUBDIR+= rcp rexec

# coreutils(textutils)
SUBDIR+= cat

# coreutils(fileutils)
SUBDIR+= chmod cp dd ln ls mkdir mv rm rmdir sync df

# coreutils(sh-utils)
SUBDIR+= date echo expr hostname pwd sleep test uname stty

# shadow
SUBDIR+= login

.include <bsd.subdir.mk>
