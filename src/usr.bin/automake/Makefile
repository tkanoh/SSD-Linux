#	$ssdlinux: Makefile,v 1.2 2002/05/27 17:00:43 kanoh Exp $

.include <bsd.own.mk>

DISTNAME=	automake
HAVE_DESTDIR=	1
CONFIGURE_FULL_PREFIX=1
CONFIGURE_ENV=
CONFIGURE_OPTIONS=
GMAKE_ENV=
GMAKE_OPTIONS=
GMAKE_INSTALL_OPTIONS= DESTDIR=${DESTDIR}
INSTALL_STRIP=	no

.include <bsd.gnu.mk>
