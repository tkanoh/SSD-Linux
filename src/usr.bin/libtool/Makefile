#	$ssdlinux: Makefile,v 1.3 2002/06/05 03:20:08 yamagata Exp $

.include <bsd.own.mk>

DISTNAME=		libtool
HAVE_DESTDIR=		1
CONFIGURE_ENV=		CFLAGS="-O2"
CONFIGURE_FULL_PREFIX=	1
CONFIGURE_OPTIONS=
GMAKE_ENV=
GMAKE_OPTIONS=
GMAKE_INSTALL_OPTIONS=	DESTDIR=${DESTDIR} bindir=${BINDIR}
INSTALL_STRIP=		no

.include <bsd.gnu.mk>
